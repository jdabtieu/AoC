#include <bits/stdc++.h>

using namespace std;

int main(int argc, char **argv) {
    int nlines;
    long long ans = 0;
    if (argc == 1) {
        freopen("../in/Day3_2023.in", "r", stdin);
        nlines = 140;
    } else {
        scanf("%d", &nlines);
    }

    for (int i = 1; i <= nlines; i++) {
        
    }
}