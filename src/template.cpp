#include <bits/stdc++.h>

using namespace std;

int main(int argc, char **argv) {
    int nlines;
    long long ans = 0;
    if (argc == 1) {
        freopen("../../in/2023/3.in", "r", stdin);
        nlines = 140;
    } else {
        scanf("%d", &nlines);
    }

    for (int i = 0; i < nlines; i++) {
        
    }
}